# Python dependencies for the extraction engine (src/main/engine.py).
# Install into the project venv:
#   py -3 -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt
#
# FFmpeg and aria2c are optional native binaries (not pip packages). The engine
# detects them at runtime and degrades gracefully when they are absent.
yt-dlp>=2026.3.17
